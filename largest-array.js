NEED TO GO THROUGH AGAIN.

/* function getLargest(numbers) {
    let no = numbers[0]
    for (let i = 0; i < numbers.length; i++) {
        const element = numbers[i];
        if (element < no) {
            no = element;
        }
        return element;
    }
}

var out = getLargest([10, 15, 25, 15]);
console.log(out); */